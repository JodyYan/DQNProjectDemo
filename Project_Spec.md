# AI 智能資產配置與 DQN 定價系統
## 專案系統規格書 (System Specification)

### 1. 專案概況
本專案為一個純前端架構的金融科技 (FinTech) 應用，旨在模擬人工智慧 (AI) 代理人透過強化學習 (DQN) 理論進行動態資產配置與個股買賣點推論。系統提供高互動性的儀表板介面，結合台灣證券交易所 (TWSE) 的真實即時數據，實現無伺服器 (Serverless) 的即時市場模擬。

### 2. 技術架構 (Technology Stack)
*   **前端框架**: React 18
*   **建置工具**: Vite
*   **樣式引擎**: Tailwind CSS
*   **資料視覺化**: 原生 SVG (DonutChart, PricingChart)
*   **非同步請求**: Fetch API 搭配 Vite Proxy 解決 CORS 限制。

### 3. 資料來源 (Data Sources)
系統採用雙軌 API 策略以兼顧效能與精準度：
1.  **全市場基礎資料庫**: 
    *   來源：TWSE Open API (`v1/exchangeReport/STOCK_DAY_ALL`)
    *   用途：系統初始化時預先載入 (Pre-fetch)，建立個股代號與名稱的 O(1) 查詢快取字典 (`stockDataCache`)。
2.  **盤中即時行情**: 
    *   來源：證交所即時資訊 API (`mis.twse.com.tw/stock/api/getStockInfo.jsp`)
    *   用途：支援上市 (`tse`) 與上櫃 (`otc`) 股票的盤中動態報價查詢，確保定價模型基於最新市價計算。

### 4. 核心功能模組 (Core Features)

#### 4.1. AI 強化學習投資組合配置
*   **動態圓餅圖 (Donut Chart)**：即時視覺化現金與各檔自選股的資金佔比，並精確計算對應的台幣金額。
*   **權重演算法**：基於使用者的「風險偏好 (1-10)」與「模擬市場狀態 (多頭/震盪/空頭)」，動態分配防禦型資產 (現金) 與高 Beta 成長股的資金權重。

#### 4.2. 狀態空間與決策解析面板
*   **理論白盒化**：打破 AI 黑箱，動態展示 DQN 獎勵函數 $R = \mu - \lambda\sigma^2$ (其中 $\lambda$ 為風險懲罰係數)。
*   **預估績效即時算式**：顯示不同市場狀態下，預期報酬 $\mu$ 與最大回撤的即時數學推導。
*   **決策語意化**：自動生成符合當前市況的策略說明 (例如：觸發避險機制，提升現金權重)。

#### 4.3. 個股邊界推論定價與視覺化
*   **定價卡片 (Pricing Card)**：
    *   計算基準：套用買入閾值公式 `市價 × [1 - 0.01 × (11 - 風險)]` 與賣出公式。
    *   貝爾曼方程式解析：展示 $Q(s, a) = r + \gamma \max Q(s', a')$ 推演邏輯，指示價值函數黃金交叉點。
*   **Q-Value 邊界圖 (Pricing Chart)**：
    *   透過 SVG 動態繪製包含買入線 (綠)、賣出線 (紅) 與市價線 (藍) 的決策範圍。
    *   模擬價格波動曲線，並於觸發買入閾值時渲染動態脈衝光圈 (Pulse)。

#### 4.4. 互動式資產池管理
*   **本金模擬**：支援任意台幣本金的即時修改，系統將於 0.1 秒內完成所有資產的金額重分配。
*   **雙軌搜尋防呆**：支援輸入「股票代號」或「公司名稱」，若輸入錯誤或股票已下市/未公開發行，系統會透過例外處理拋出明確的診斷回報。
*   **標的增刪**：加入新標的時強制執行 API 報價驗證，確認真實存在後方可加入自選池；標籤支援一鍵刪除。

---
*Generated by Antigravity AI Assistant*
